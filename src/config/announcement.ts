/**
 * Landing-page announcement banner.
 *
 * Flip `enabled` to show or hide the announcement pill on the home page.
 * Update `text` and `link` to point at whatever you're announcing.
 */
export const announcement = {
  /** Show the announcement banner on the landing page. */
  enabled: true,

  /** Small highlighted label before the message. Set to '' to hide it. */
  tag: 'New',

  /** The message — keep it short, it shows on a single line. */
  text: 'CodeceptJS 4.0 is here',

  /** Where the banner links to. */
  link: '/blog/codeceptjs-4/',
};
