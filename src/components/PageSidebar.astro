---
import Default from '@astrojs/starlight/components/PageSidebar.astro';

const route = Astro.locals.starlightRoute ?? {};
const slug = route.entry?.slug ?? '';

const is404Page = slug === '404' || slug.endsWith('/404');
---

{!is404Page && <Default />}